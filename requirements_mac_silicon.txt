opencv-python>=4.8.0
imageio>=2.31.0
Pillow>=10.0.0
numpy>=1.22
OpenEXR>=3.2.0; python_version < "3.14"
# Imath ships inside the OpenEXR wheel — do NOT add a separate Imath dependency (PyPI imath maxes at 0.0.2).
OpenImageIO>=3.0.0
# DPX has no Pillow plugin; OpenImageIO is the VFX-industry-standard library for it.
opencolorio>=2.3.0
transformers>=4.36.0
# Multipass Estimate runs Marigold IID (materials and lighting) through diffusers;
# accelerate lets its 1.7 GB UNets load without a second full-size copy in RAM.
diffusers>=0.33.0
accelerate>=0.26.0
scipy>=1.10.0
defusedxml>=0.7.1
imageio-ffmpeg>=0.4.9
tqdm>=4.66.0
aiohttp>=3.8.0
tifffile>=2023.7.10
